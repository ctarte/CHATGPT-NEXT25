import Stripe from 'stripe';import {serverEnv} from '@/lib/server/env';export async function POST(req:Request){const e=serverEnv(),stripe=new Stripe(e.stripeSecret),sig=req.headers.get('stripe-signature');if(!sig)return new Response('Missing signature',{status:400});let event:Stripe.Event;try{event=stripe.webhooks.constructEvent(await req.text(),sig,e.stripeWebhook)}catch{return new Response('Invalid signature',{status:400})}if(event.type==='checkout.session.completed'){/* TODO: idempotently verify expected product/price/payment state, persist order, grant entitlement, queue welcome. */}return new Response('ok')}