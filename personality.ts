import type {Signal,Friction} from './types'; export type WorkStyleProfile={version:string;signals:Signal[];notes:string[]}; export function buildWorkStyleProfile(signals:Signal[]):WorkStyleProfile{return{version:'pws-1.0',signals:signals.map(s=>({...s,value:Math.max(-1,Math.min(1,s.value)),confidence:Math.max(0,Math.min(1,s.confidence))})),notes:['Directional work-style profile; not a clinical or psychological diagnosis.','No single dimension automatically excludes an opportunity.']}} export function frictionRule(dimension:string,why:string,designAround:string[],severity:'low'|'medium'|'high'='medium'):Friction{return{dimension,severity,why,designAround}}